<% content_for(:title, "スレッドを作成する") %>

<div class="m-3 mt-5">
  <h2 class="mb-5">スレッドを作成する</h2>

  <%= simple_form_for [:admins, @room] do |f| %>
    <%= f.input :name, required: false %>
    <div class="text-center">
       <%= f.button :submit, '作成する', class: "btn btn-success" %>
    </div>
  <% end %>
</div>