<%= @question.title %>

<%= simple_form_for [@question, @answer] do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.button :submit, '回答する' %>
<% end %>

<% @question.answers.each do |answer| %>
  <%= answer.name %>
  <%= answer.description %>
<% end %>
