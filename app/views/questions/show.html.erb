<%= @question.title %>

<%= simple_form_for [@question, Answer.new] do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.button :submit, '回答する' %>
<% end %>

<% @question.answer.each do |answer| %>
  <%= answer.name %>
  <%= answer.description %>
<% end %>
